// TikTok official APIs are restricted; here we provide a placeholder that returns []
// To enable TikTok you'll need a developer integration or a 3rd-party aggregator.
async function fetchHashtag(tag){
  // Placeholder: return empty array if no integration is configured
  // If you integrate a TikTok scraping service or official API, implement it here to return unified items.
  return [];
}

module.exports = { fetchHashtag };
